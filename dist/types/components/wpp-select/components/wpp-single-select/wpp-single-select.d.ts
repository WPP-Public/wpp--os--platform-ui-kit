import { WppSelect } from '../../wpp-select';
import { SelectSize } from '../../types';
export declare function renderSingleSelect(this: WppSelect, isBaseComponent?: boolean, customSize?: SelectSize): any;
