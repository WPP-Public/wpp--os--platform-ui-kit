import { WppSelect } from '@platform-ui-kit/components-library-react'
import styles from './SelectsPerfExample.module.scss'
import React from 'react'
import { OPTIONS_LIST } from './const'

export const SelectsPerfExample = () => (
  <div className={styles.container}>
    <div className={styles.row}>
      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        data-testid="focus-multiple-select"
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>
    </div>

    <div className={styles.row}>
      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>
    </div>

    <div className={styles.row}>
      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>
    </div>

    <div className={styles.row}>
      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>
    </div>
    <div className={styles.row}>
      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        data-testid="focus-multiple-select"
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>
    </div>

    <div className={styles.row}>
      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>
    </div>

    <div className={styles.row}>
      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>
    </div>

    <div className={styles.row}>
      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>
    </div>
    <div className={styles.row}>
      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        data-testid="focus-multiple-select"
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>
    </div>

    <div className={styles.row}>
      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>
    </div>

    <div className={styles.row}>
      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>
    </div>

    <div className={styles.row}>
      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>
    </div>
    <div className={styles.row}>
      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        data-testid="focus-multiple-select"
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>
    </div>

    <div className={styles.row}>
      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>
    </div>

    <div className={styles.row}>
      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>
    </div>

    <div className={styles.row}>
      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>
    </div>
    <div className={styles.row}>
      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        data-testid="focus-multiple-select"
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>
    </div>

    <div className={styles.row}>
      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>
    </div>

    <div className={styles.row}>
      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>
    </div>

    <div className={styles.row}>
      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>

      <WppSelect
        placeholder="Choose options"
        type="multiple"
        required
        withFolder
        withSearch
        labelConfig={{ text: 'Multiple Select with search' }}
        className={styles.item}
        value={OPTIONS_LIST}
        list={OPTIONS_LIST.map(option => ({ value: option, label: option }))}
      ></WppSelect>
    </div>
  </div>
)
