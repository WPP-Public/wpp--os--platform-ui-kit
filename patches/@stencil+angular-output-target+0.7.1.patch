diff --git a/node_modules/@stencil/angular-output-target/dist/generate-angular-component.js b/node_modules/@stencil/angular-output-target/dist/generate-angular-component.js
index b1b095f..b520e36 100644
--- a/node_modules/@stencil/angular-output-target/dist/generate-angular-component.js
+++ b/node_modules/@stencil/angular-output-target/dist/generate-angular-component.js
@@ -9,7 +9,7 @@ import { createComponentEventTypeImports, dashToPascalCase, formatToQuotedList }
  * @param includeImportCustomElements Whether to define the component as a custom element.
  * @returns The component declaration as a string.
  */
-export const createAngularComponentDefinition = (tagName, inputs, outputs, methods, includeImportCustomElements = false) => {
+export const createAngularComponentDefinition = (tagName, inputs, outputs, methods, includeImportCustomElements = false, registryTagName) => {
     const tagNameAsPascal = dashToPascalCase(tagName);
     const hasInputs = inputs.length > 0;
     const hasOutputs = outputs.length > 0;
@@ -40,7 +40,7 @@ export const createAngularComponentDefinition = (tagName, inputs, outputs, metho
      */
     const output = `@ProxyCmp({${proxyCmpOptions.join(',')}\n})
 @Component({
-  selector: '${tagName}',
+  selector: '${registryTagName}',
   changeDetection: ChangeDetectionStrategy.OnPush,
   template: '<ng-content></ng-content>',
   // eslint-disable-next-line @angular-eslint/no-inputs-metadata-property
diff --git a/node_modules/@stencil/angular-output-target/dist/generate-value-accessors.d.ts b/node_modules/@stencil/angular-output-target/dist/generate-value-accessors.d.ts
index 3c318e1..c7363c4 100644
--- a/node_modules/@stencil/angular-output-target/dist/generate-value-accessors.d.ts
+++ b/node_modules/@stencil/angular-output-target/dist/generate-value-accessors.d.ts
@@ -5,4 +5,4 @@ export interface ValueAccessor {
     eventTargets: [string, string][];
 }
 export default function generateValueAccessors(compilerCtx: CompilerCtx, components: ComponentCompilerMeta[], outputTarget: OutputTargetAngular, config: Config): Promise<void>;
-export declare function createValueAccessor(srcFileContents: string, valueAccessor: ValueAccessor): string;
+export declare function createValueAccessor(srcFileContents: string, valueAccessor: ValueAccessor, config: Config): string;
diff --git a/node_modules/@stencil/angular-output-target/dist/generate-value-accessors.js b/node_modules/@stencil/angular-output-target/dist/generate-value-accessors.js
index 6619bd2..69af677 100644
--- a/node_modules/@stencil/angular-output-target/dist/generate-value-accessors.js
+++ b/node_modules/@stencil/angular-output-target/dist/generate-value-accessors.js
@@ -25,17 +25,24 @@ export default async function generateValueAccessors(compilerCtx, components, ou
         const targetFilePath = path.join(targetDir, targetFileName);
         const srcFilePath = path.join(__dirname, '../resources/control-value-accessors/', targetFileName);
         const srcFileContents = await compilerCtx.fs.readFile(srcFilePath);
-        const finalText = createValueAccessor(srcFileContents, normalizedValueAccessors[valueAccessorType]);
+        const finalText = createValueAccessor(srcFileContents, normalizedValueAccessors[valueAccessorType], config);
         await compilerCtx.fs.writeFile(targetFilePath, finalText);
     }));
     await copyResources(config, ['value-accessor.ts'], targetDir);
 }
-export function createValueAccessor(srcFileContents, valueAccessor) {
+export function createValueAccessor(srcFileContents, valueAccessor, config) {
     const hostContents = valueAccessor.eventTargets.map((listItem) => VALUE_ACCESSOR_EVENTTARGETS.replace(VALUE_ACCESSOR_EVENT, listItem[0]).replace(VALUE_ACCESSOR_TARGETATTR, listItem[1]));
     return srcFileContents
-        .replace(VALUE_ACCESSOR_SELECTORS, valueAccessor.elementSelectors.join(', '))
+        .replace(VALUE_ACCESSOR_SELECTORS, valueAccessor.elementSelectors
+        .map(selector => { var _a; return addTagNamePostfix(selector, (_a = config.flags) === null || _a === void 0 ? void 0 : _a.tagNamePostfix); }).join(', '))
         .replace(VALUE_ACCESSOR_EVENTTARGETS, hostContents.join(`,${EOL}`));
 }
+function addTagNamePostfix(tagName, postfix) {
+    if (postfix) {
+        return `${tagName}-${postfix}`;
+    }
+        return tagName;
+}
 function copyResources(config, resourcesFilesToCopy, directory) {
     if (!config.sys || !config.sys.copy) {
         throw new Error('stencil is not properly intialized at this step. Notify the developer');
diff --git a/node_modules/@stencil/angular-output-target/dist/index.cjs.js b/node_modules/@stencil/angular-output-target/dist/index.cjs.js
index 7d8a725..7b1f6d1 100644
--- a/node_modules/@stencil/angular-output-target/dist/index.cjs.js
+++ b/node_modules/@stencil/angular-output-target/dist/index.cjs.js
@@ -140,7 +140,7 @@ const SLASH_REGEX = /\\/g;
  * @param includeImportCustomElements Whether to define the component as a custom element.
  * @returns The component declaration as a string.
  */
-const createAngularComponentDefinition = (tagName, inputs, outputs, methods, includeImportCustomElements = false) => {
+const createAngularComponentDefinition = (tagName, inputs, outputs, methods, includeImportCustomElements = false, registryTagName) => {
     const tagNameAsPascal = dashToPascalCase(tagName);
     const hasInputs = inputs.length > 0;
     const hasOutputs = outputs.length > 0;
@@ -171,7 +171,7 @@ const createAngularComponentDefinition = (tagName, inputs, outputs, methods, inc
      */
     const output = `@ProxyCmp({${proxyCmpOptions.join(',')}\n})
 @Component({
-  selector: '${tagName}',
+  selector: '${registryTagName}',
   changeDetection: ChangeDetectionStrategy.OnPush,
   template: '<ng-content></ng-content>',
   // eslint-disable-next-line @angular-eslint/no-inputs-metadata-property
@@ -309,17 +309,24 @@ async function generateValueAccessors(compilerCtx, components, outputTarget, con
         const targetFilePath = path__default['default'].join(targetDir, targetFileName);
         const srcFilePath = path__default['default'].join(__dirname, '../resources/control-value-accessors/', targetFileName);
         const srcFileContents = await compilerCtx.fs.readFile(srcFilePath);
-        const finalText = createValueAccessor(srcFileContents, normalizedValueAccessors[valueAccessorType]);
+        const finalText = createValueAccessor(srcFileContents, normalizedValueAccessors[valueAccessorType], config);
         await compilerCtx.fs.writeFile(targetFilePath, finalText);
     }));
     await copyResources(config, ['value-accessor.ts'], targetDir);
 }
-function createValueAccessor(srcFileContents, valueAccessor) {
+function createValueAccessor(srcFileContents, valueAccessor, config) {
     const hostContents = valueAccessor.eventTargets.map((listItem) => VALUE_ACCESSOR_EVENTTARGETS.replace(VALUE_ACCESSOR_EVENT, listItem[0]).replace(VALUE_ACCESSOR_TARGETATTR, listItem[1]));
     return srcFileContents
-        .replace(VALUE_ACCESSOR_SELECTORS, valueAccessor.elementSelectors.join(', '))
+        .replace(VALUE_ACCESSOR_SELECTORS, valueAccessor.elementSelectors
+        .map(selector => { var _a; return addTagNamePostfix(selector, (_a = config.flags) === null || _a === void 0 ? void 0 : _a.tagNamePostfix); }).join(', '))
         .replace(VALUE_ACCESSOR_EVENTTARGETS, hostContents.join(`,${os.EOL}`));
 }
+function addTagNamePostfix(tagName, postfix) {
+    if (postfix) {
+        return `${tagName}-${postfix}`;
+    }
+    return tagName;
+}
 function copyResources(config, resourcesFilesToCopy, directory) {
     if (!config.sys || !config.sys.copy) {
         throw new Error('stencil is not properly intialized at this step. Notify the developer');
@@ -480,7 +487,7 @@ ${createImportStatement(componentLibImports, './angular-component-lib/utils')}\n
          * 2. Optionally the @NgModule decorated class (if includeSingleComponentAngularModules is true)
          * 3. The component interface (using declaration merging for types).
          */
-        const componentDefinition = createAngularComponentDefinition(cmpMeta.tagName, inputs, outputs, methods, includeImportCustomElements);
+        const componentDefinition = createAngularComponentDefinition(cmpMeta.tagName, inputs, outputs, methods, includeImportCustomElements, cmpMeta.registryTagName);
         const moduleDefinition = generateAngularModuleForComponent(cmpMeta.tagName);
         const componentTypeDefinition = createComponentTypeDefinition(tagNameAsPascal, cmpMeta.events, componentCorePackage, includeImportCustomElements, customElementsDir);
         proxyFileOutput.push(componentDefinition, '\n');
diff --git a/node_modules/@stencil/angular-output-target/dist/index.js b/node_modules/@stencil/angular-output-target/dist/index.js
index df5aeb9..831d907 100644
--- a/node_modules/@stencil/angular-output-target/dist/index.js
+++ b/node_modules/@stencil/angular-output-target/dist/index.js
@@ -132,7 +132,7 @@ const SLASH_REGEX = /\\/g;
  * @param includeImportCustomElements Whether to define the component as a custom element.
  * @returns The component declaration as a string.
  */
-const createAngularComponentDefinition = (tagName, inputs, outputs, methods, includeImportCustomElements = false) => {
+const createAngularComponentDefinition = (tagName, inputs, outputs, methods, includeImportCustomElements = false, registryTagName) => {
     const tagNameAsPascal = dashToPascalCase(tagName);
     const hasInputs = inputs.length > 0;
     const hasOutputs = outputs.length > 0;
@@ -163,7 +163,7 @@ const createAngularComponentDefinition = (tagName, inputs, outputs, methods, inc
      */
     const output = `@ProxyCmp({${proxyCmpOptions.join(',')}\n})
 @Component({
-  selector: '${tagName}',
+  selector: '${registryTagName}',
   changeDetection: ChangeDetectionStrategy.OnPush,
   template: '<ng-content></ng-content>',
   // eslint-disable-next-line @angular-eslint/no-inputs-metadata-property
@@ -301,17 +301,24 @@ async function generateValueAccessors(compilerCtx, components, outputTarget, con
         const targetFilePath = path.join(targetDir, targetFileName);
         const srcFilePath = path.join(__dirname, '../resources/control-value-accessors/', targetFileName);
         const srcFileContents = await compilerCtx.fs.readFile(srcFilePath);
-        const finalText = createValueAccessor(srcFileContents, normalizedValueAccessors[valueAccessorType]);
+        const finalText = createValueAccessor(srcFileContents, normalizedValueAccessors[valueAccessorType], config);
         await compilerCtx.fs.writeFile(targetFilePath, finalText);
     }));
     await copyResources(config, ['value-accessor.ts'], targetDir);
 }
-function createValueAccessor(srcFileContents, valueAccessor) {
+function createValueAccessor(srcFileContents, valueAccessor, config) {
     const hostContents = valueAccessor.eventTargets.map((listItem) => VALUE_ACCESSOR_EVENTTARGETS.replace(VALUE_ACCESSOR_EVENT, listItem[0]).replace(VALUE_ACCESSOR_TARGETATTR, listItem[1]));
     return srcFileContents
-        .replace(VALUE_ACCESSOR_SELECTORS, valueAccessor.elementSelectors.join(', '))
+        .replace(VALUE_ACCESSOR_SELECTORS, valueAccessor.elementSelectors
+        .map(selector => { var _a; return addTagNamePostfix(selector, (_a = config.flags) === null || _a === void 0 ? void 0 : _a.tagNamePostfix); }).join(', '))
         .replace(VALUE_ACCESSOR_EVENTTARGETS, hostContents.join(`,${EOL}`));
 }
+function addTagNamePostfix(tagName, postfix) {
+    if (postfix) {
+        return `${tagName}-${postfix}`;
+    }
+    return tagName;
+}
 function copyResources(config, resourcesFilesToCopy, directory) {
     if (!config.sys || !config.sys.copy) {
         throw new Error('stencil is not properly intialized at this step. Notify the developer');
@@ -472,7 +479,7 @@ ${createImportStatement(componentLibImports, './angular-component-lib/utils')}\n
          * 2. Optionally the @NgModule decorated class (if includeSingleComponentAngularModules is true)
          * 3. The component interface (using declaration merging for types).
          */
-        const componentDefinition = createAngularComponentDefinition(cmpMeta.tagName, inputs, outputs, methods, includeImportCustomElements);
+        const componentDefinition = createAngularComponentDefinition(cmpMeta.tagName, inputs, outputs, methods, includeImportCustomElements, cmpMeta.registryTagName);
         const moduleDefinition = generateAngularModuleForComponent(cmpMeta.tagName);
         const componentTypeDefinition = createComponentTypeDefinition(tagNameAsPascal, cmpMeta.events, componentCorePackage, includeImportCustomElements, customElementsDir);
         proxyFileOutput.push(componentDefinition, '\n');
diff --git a/node_modules/@stencil/angular-output-target/dist/output-angular.js b/node_modules/@stencil/angular-output-target/dist/output-angular.js
index c84fc38..1b2120f 100644
--- a/node_modules/@stencil/angular-output-target/dist/output-angular.js
+++ b/node_modules/@stencil/angular-output-target/dist/output-angular.js
@@ -128,7 +128,7 @@ ${createImportStatement(componentLibImports, './angular-component-lib/utils')}\n
          * 2. Optionally the @NgModule decorated class (if includeSingleComponentAngularModules is true)
          * 3. The component interface (using declaration merging for types).
          */
-        const componentDefinition = createAngularComponentDefinition(cmpMeta.tagName, inputs, outputs, methods, includeImportCustomElements);
+        const componentDefinition = createAngularComponentDefinition(cmpMeta.tagName, inputs, outputs, methods, includeImportCustomElements, cmpMeta.registryTagName);
         const moduleDefinition = generateAngularModuleForComponent(cmpMeta.tagName);
         const componentTypeDefinition = createComponentTypeDefinition(tagNameAsPascal, cmpMeta.events, componentCorePackage, includeImportCustomElements, customElementsDir);
         proxyFileOutput.push(componentDefinition, '\n');
