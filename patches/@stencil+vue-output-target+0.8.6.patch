diff --git a/node_modules/@stencil/vue-output-target/dist/generate-vue-component.js b/node_modules/@stencil/vue-output-target/dist/generate-vue-component.js
index 3f329e5..5313e6b 100644
--- a/node_modules/@stencil/vue-output-target/dist/generate-vue-component.js
+++ b/node_modules/@stencil/vue-output-target/dist/generate-vue-component.js
@@ -13,7 +13,7 @@ export const createComponentDefinition = (importTypes, componentModelConfig, inc
     const findModel = componentModelConfig && componentModelConfig.find((config) => config.elements.includes(cmpMeta.tagName));
     const modelType = findModel !== undefined ? `, ${componentType}["${findModel.targetAttr}"]` : '';
     let templateString = `
-export const ${tagNameAsPascal} = /*@__PURE__*/ defineContainer<${componentType}${modelType}>('${cmpMeta.tagName}', ${importAs}`;
+export const ${tagNameAsPascal} = /*@__PURE__*/ defineContainer<${componentType}${modelType}>('${cmpMeta.registryTagName}', ${importAs}`;
     if (props.length > 0) {
         templateString += `, [
   ${props.length > 0 ? props.join(',\n  ') : ''}
diff --git a/node_modules/@stencil/vue-output-target/dist/index.cjs.js b/node_modules/@stencil/vue-output-target/dist/index.cjs.js
index be292b0..ea83ed4 100644
--- a/node_modules/@stencil/vue-output-target/dist/index.cjs.js
+++ b/node_modules/@stencil/vue-output-target/dist/index.cjs.js
@@ -101,7 +101,7 @@ const createComponentDefinition = (importTypes, componentModelConfig, includeCus
     const findModel = componentModelConfig && componentModelConfig.find((config) => config.elements.includes(cmpMeta.tagName));
     const modelType = findModel !== undefined ? `, ${componentType}["${findModel.targetAttr}"]` : '';
     let templateString = `
-export const ${tagNameAsPascal} = /*@__PURE__*/ defineContainer<${componentType}${modelType}>('${cmpMeta.tagName}', ${importAs}`;
+export const ${tagNameAsPascal} = /*@__PURE__*/ defineContainer<${componentType}${modelType}>('${cmpMeta.registryTagName}', ${importAs}`;
     if (props.length > 0) {
         templateString += `, [
   ${props.length > 0 ? props.join(',\n  ') : ''}
diff --git a/node_modules/@stencil/vue-output-target/dist/index.js b/node_modules/@stencil/vue-output-target/dist/index.js
index 1e1b3bd..e49c094 100644
--- a/node_modules/@stencil/vue-output-target/dist/index.js
+++ b/node_modules/@stencil/vue-output-target/dist/index.js
@@ -92,7 +92,7 @@ const createComponentDefinition = (importTypes, componentModelConfig, includeCus
     const findModel = componentModelConfig && componentModelConfig.find((config) => config.elements.includes(cmpMeta.tagName));
     const modelType = findModel !== undefined ? `, ${componentType}["${findModel.targetAttr}"]` : '';
     let templateString = `
-export const ${tagNameAsPascal} = /*@__PURE__*/ defineContainer<${componentType}${modelType}>('${cmpMeta.tagName}', ${importAs}`;
+export const ${tagNameAsPascal} = /*@__PURE__*/ defineContainer<${componentType}${modelType}>('${cmpMeta.registryTagName}', ${importAs}`;
     if (props.length > 0) {
         templateString += `, [
   ${props.length > 0 ? props.join(',\n  ') : ''}
diff --git a/node_modules/@stencil/vue-output-target/vue-component-lib/utils.ts b/node_modules/@stencil/vue-output-target/vue-component-lib/utils.ts
index 826e78e..62651ac 100644
--- a/node_modules/@stencil/vue-output-target/vue-component-lib/utils.ts
+++ b/node_modules/@stencil/vue-output-target/vue-component-lib/utils.ts
@@ -60,7 +60,7 @@ export const defineContainer = <Props, VModelType = string | number | boolean>(
   name: string,
   defineCustomElement: any,
   componentProps: string[] = [],
-  modelProp?: string,
+  modelProp: string = 'value',
   modelUpdateEvent?: string,
   externalModelUpdateEvent?: string
 ) => {
@@ -78,12 +78,17 @@ export const defineContainer = <Props, VModelType = string | number | boolean>(
     let modelPropValue = props[modelProp];
     const containerRef = ref<HTMLElement>();
     const classes = new Set(getComponentClasses(attrs.class));
+
+    // wpp start
+    const wppEvents = componentProps.filter(prop => prop.match(/wpp/gi))
+    // wpp end
+
     const onVnodeBeforeMount = (vnode: VNode) => {
       // Add a listener to tell Vue to update the v-model
       if (vnode.el) {
-        const eventsNames = Array.isArray(modelUpdateEvent) ? modelUpdateEvent : [modelUpdateEvent];
-        eventsNames.forEach((eventName: string) => {
-          vnode.el!.addEventListener(eventName.toLowerCase(), (e: Event) => {
+        // const eventsNames = Array.isArray(modelUpdateEvent) ? modelUpdateEvent : [modelUpdateEvent];
+        wppEvents.forEach((eventName: string) => {
+          vnode.el!.addEventListener(eventName, (e: Event) => {
             modelPropValue = (e?.target as any)[modelProp];
             emit(UPDATE_VALUE_EVENT, modelPropValue);
 
@@ -95,9 +100,12 @@ export const defineContainer = <Props, VModelType = string | number | boolean>(
              * native web component, but the v-model will
              * not have been updated yet.
              */
-            if (externalModelUpdateEvent) {
-              emit(externalModelUpdateEvent, e);
-            }
+
+            // if (externalModelUpdateEvent) {
+            //   emit(externalModelUpdateEvent, e);
+            // }
+
+            emit(eventName, e);
           });
         });
       }
@@ -146,7 +154,7 @@ export const defineContainer = <Props, VModelType = string | number | boolean>(
         ref: containerRef,
         class: getElementClasses(containerRef, classes),
         onClick: handleClick,
-        onVnodeBeforeMount: modelUpdateEvent ? onVnodeBeforeMount : undefined,
+        onVnodeBeforeMount: modelUpdateEvent || wppEvents.length ? onVnodeBeforeMount : undefined,
       };
 
       /**
